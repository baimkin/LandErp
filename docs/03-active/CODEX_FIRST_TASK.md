# Готовое задание для Codex — первый этап

Скопируйте текст ниже в новую задачу Codex после локального клонирования
репозитория.

---

Продолжаем проект LandErp.

Выполни только `GATE-SPIKE-001-01 — Каркас и контракты` в ветке
`spike/001-avito-viability`.

Этим сообщением я утверждаю выполнение только данного Gate. Следующие Gate не
разрешены.

Сначала прочитай строго по порядку:

1. `AGENTS.md`;
2. `docs/START_HERE.md`;
3. `docs/03-active/ACTIVE_TASK.md`;
4. документы из `Required reading` активной задачи.

Не читай остальные планы без конкретной необходимости. Не реализуй Playwright-
навигацию, Avito-селекторы, HTML-fixtures, сервер, БД, UI или production Agent.

Перед первой записью:

- проверь состояние Git и ветку;
- кратко перечисли понимание scope;
- перечисли планируемые файлы, зависимости и проверки;
- дождись моего подтверждения записи/команд, если среда его запрашивает.

После реализации выполни все проверки из Gate, создай
`docs/03-active/reports/GATE-SPIKE-001-01_REPORT.md`, покажи итог и остановись.
Не меняй активную задачу на Gate 02 и не начинай следующую работу.

---
