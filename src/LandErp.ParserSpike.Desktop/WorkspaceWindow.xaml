<Window FontFamily="Segoe UI" FontSize="13.5" Foreground="#203047" Background="#F4F6FA"
    x:Class="LandErp.ParserSpike.Desktop.WorkspaceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LandErp.ParserSpike.Desktop"
    Title="LandErp Parser" Width="1440" Height="900" MinWidth="980" MinHeight="680">
<Window.Resources><ResourceDictionary><ResourceDictionary.MergedDictionaries><ResourceDictionary Source="ParserTheme.xaml"/></ResourceDictionary.MergedDictionaries></ResourceDictionary></Window.Resources>
<DockPanel Margin="16">
<Grid DockPanel.Dock="Top" Margin="0,0,0,10">
  <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="200"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
  <StackPanel><TextBlock Text="LandErp Parser" Style="{StaticResource Title}"/><TextBlock x:Name="ModeDescription" Style="{StaticResource Hint}"/></StackPanel>
  <StackPanel Grid.Column="1" Margin="14,0,8,0"><TextBlock Text="Где работать" Style="{StaticResource Hint}"/><ComboBox x:Name="ModeInput" SelectionChanged="ModeChanged"><ComboBoxItem Content="На этом компьютере"/><ComboBoxItem Content="Через сервер"/></ComboBox></StackPanel>
  <StackPanel Grid.Column="2" VerticalAlignment="Center"><Button x:Name="AutomationButton" Content="Включить автоработу" Style="{StaticResource Primary}" Click="AutomationClick"/><TextBlock x:Name="ConnectionText" Style="{StaticResource Hint}" MaxWidth="280"/></StackPanel>
</Grid>

<Border DockPanel.Dock="Bottom" Background="#EAF0FA" CornerRadius="7" Padding="9" Margin="0,10,0,0"><TextBlock x:Name="StatusText" Text="Добавьте поиск или откройте сайт, чтобы настроить фильтры."/></Border>

<TabControl x:Name="Pages" BorderThickness="0" Background="Transparent">
<TabItem Header="Поиски"><Grid Margin="0,10,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
  <Border Style="{StaticResource Card}" Padding="10"><DockPanel>
    <StackPanel DockPanel.Dock="Top"><TextBlock Text="Группы" Style="{StaticResource SectionTitle}"/><Button Content="+ Новая группа" Click="AddGroupClick"/></StackPanel>
    <StackPanel DockPanel.Dock="Bottom"><Button Content="Переименовать" Click="RenameGroupClick"/><Button Content="Архивировать группу" Click="ArchiveGroupClick"/></StackPanel>
    <ListBox x:Name="GroupsList" DisplayMemberPath="Name" BorderThickness="0" SelectionChanged="GroupSelected"/>
  </DockPanel></Border>
  <Border Grid.Column="2" Style="{StaticResource Card}"><DockPanel>
    <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,8"><Button x:Name="AddButton" Content="+ Добавить поиск" Style="{StaticResource Primary}" Click="AddClick"/><Button Content="Открыть сайт" Click="BrowseClick"/><Button Content="Добавить из браузера" Click="CaptureClick"/><Button Content="Обновить" Click="RefreshClick"/></WrapPanel>
    <WrapPanel DockPanel.Dock="Bottom" Margin="0,8,0,0"><Button x:Name="StartButton" Content="Запустить выбранный" Click="RunClick"/><Button Content="Изменить" Click="EditClick"/><Button Content="Включить / приостановить" Click="ToggleSearchClick"/><Button x:Name="ArchiveButton" Content="В архив" Click="ArchiveClick"/><Button x:Name="TransferButton" Content="Добавить на сервер…" Click="TransferClick"/></WrapPanel>
    <Grid><DataGrid x:Name="LinksGrid"><DataGrid.Columns><DataGridTextColumn Header="Поиск" Binding="{Binding Label}" Width="2*"/><DataGridTextColumn Header="Источник" Binding="{Binding Source}" Width="90"/><DataGridTextColumn Header="Расписание" Binding="{Binding ScheduleText}" Width="2*"/><DataGridTextColumn Header="Состояние" Binding="{Binding State}" Width="130"/></DataGrid.Columns></DataGrid><TextBlock x:Name="EmptyText" Text="Здесь пока нет поисков. Добавьте ссылку или настройте поиск в браузере." Foreground="{StaticResource Muted}" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="380"/></Grid>
  </DockPanel></Border>
</Grid></TabItem>

<TabItem Header="Работа"><Border Style="{StaticResource Card}" Margin="0,10,0,0"><DockPanel>
  <StackPanel DockPanel.Dock="Top"><TextBlock Text="Текущая работа и история" Style="{StaticResource SectionTitle}"/><TextBlock Text="Авторабота запускает местные расписания или принимает задания сервера. Приложение должно оставаться открытым." Style="{StaticResource Hint}"/><WrapPanel><Button Content="Пауза текущего сбора" Click="PauseClick"/><Button Content="Продолжить" Click="ResumeClick"/><Button Content="Остановить всё" Click="StopClick"/></WrapPanel><Border x:Name="NoticePanel" Visibility="Collapsed" Background="#FFF4D7" Padding="10" Margin="0,8"><StackPanel><TextBlock x:Name="NoticeText"/><Button Content="Проверка в браузере выполнена — продолжить" HorizontalAlignment="Left" Click="VerifyClick"/></StackPanel></Border></StackPanel>
  <DataGrid x:Name="JobsGrid"><DataGrid.Columns><DataGridTextColumn Header="Поиск" Binding="{Binding Label}" Width="2*"/><DataGridTextColumn Header="Источник" Binding="{Binding Source}" Width="90"/><DataGridTextColumn Header="Состояние" Binding="{Binding State}" Width="160"/><DataGridTextColumn Header="Прогресс" Binding="{Binding Progress}" Width="190"/><DataGridTextColumn Header="Начало" Binding="{Binding Started}" Width="140"/></DataGrid.Columns></DataGrid>
</DockPanel></Border></TabItem>

<TabItem Header="Результаты"><Border Style="{StaticResource Card}" Margin="0,10,0,0" Padding="12"><DockPanel>
  <StackPanel DockPanel.Dock="Top">
    <WrapPanel x:Name="ResultsToolbar">
      <TextBox x:Name="SearchInput" Style="{StaticResource ToolbarTextBox}" Width="220" ToolTip="Название, адрес, продавец, ID или кадастровый номер"/>
      <ComboBox x:Name="SourceFilterInput" Style="{StaticResource ToolbarComboBox}" Width="112" DisplayMemberPath="Label" ToolTip="Источник"/>
      <ComboBox x:Name="SearchFilterInput" Style="{StaticResource ToolbarComboBox}" Width="170" DisplayMemberPath="Label" ToolTip="Поисковая ссылка"/>
      <ComboBox x:Name="RunFilterInput" Style="{StaticResource ToolbarComboBox}" Width="180" DisplayMemberPath="Label" ToolTip="Запуск"/>
      <ComboBox x:Name="QualityFilterInput" Style="{StaticResource ToolbarComboBox}" Width="185" DisplayMemberPath="Label" ToolTip="Качество данных"/>
      <Button Content="Найти" Style="{StaticResource ToolbarButton}" Click="FindClick"/>
      <Button x:Name="ResetFiltersButton" Content="Сбросить" Style="{StaticResource ToolbarButton}" Click="ResetFiltersClick"/>
      <Button Content="CSV" Style="{StaticResource ToolbarButton}" Click="ExportClick" ToolTip="Экспорт текущей выборки в CSV"/>
    </WrapPanel>
    <TextBlock Text="⚠ в колонке «Тип» означает расхождение между заявленным источником и найденным в тексте." Style="{StaticResource Hint}" Margin="0,0,0,4"/>
  </StackPanel>

  <WrapPanel DockPanel.Dock="Bottom" Margin="0,8,0,0"><Button Content="Назад" Click="PreviousClick"/><TextBlock x:Name="PageText" VerticalAlignment="Center" Margin="8,0"/><Button Content="Далее" Click="NextClick"/></WrapPanel>

  <Grid Margin="0,4,0,0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition x:Name="ResultsColumn" Width="*" MinWidth="420"/>
      <ColumnDefinition x:Name="DetailsSplitterColumn" Width="6"/>
      <ColumnDefinition x:Name="DetailsColumn" Width="430" MaxWidth="760"/>
    </Grid.ColumnDefinitions>

    <DataGrid x:Name="ListingsGrid" SelectionChanged="ListingSelected" HorizontalScrollBarVisibility="Auto"
              VerticalScrollBarVisibility="Auto" FrozenColumnCount="1" EnableColumnVirtualization="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Объявление" Binding="{Binding Title}" Width="240" MinWidth="180"/>
        <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="115"/>
        <DataGridTextColumn Header="Площадь" Binding="{Binding Area}" Width="90"/>
        <DataGridTextColumn Header="₽ / сот." Binding="{Binding PricePerSotka}" Width="115"/>
        <DataGridTextColumn Header="Адрес" Binding="{Binding Location}" Width="260" MinWidth="180"/>
        <DataGridTextColumn Header="Тип" Binding="{Binding LandTypes}" Width="130"/>
        <DataGridTextColumn Header="!" Binding="{Binding LandTypeIssue}" Width="34"/>
        <DataGridTextColumn Header="Опубликовано" Binding="{Binding Published}" Width="120"/>
        <DataGridTemplateColumn Header="Парсинг" Width="112">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate><TextBlock Text="{Binding LastParsed}" ToolTip="{Binding LastParsedFull}" VerticalAlignment="Center"/></DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="Фото" Binding="{Binding PhotoCount}" Width="52"/>
        <DataGridTextColumn Header="Кадастровый №" Binding="{Binding CadastralNumber}" Width="145"/>
      </DataGrid.Columns>
    </DataGrid>

    <GridSplitter x:Name="DetailsSplitter" Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"
                  Background="#E5EAF2" ShowsPreview="False" DragCompleted="DetailsSplitterDragCompleted"/>

    <local:ListingDetailsControl x:Name="DetailsPanel" Grid.Column="2" Margin="10,0,0,0"/>
  </Grid>
</DockPanel></Border></TabItem>

<TabItem Header="Настройки"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="0,10" MaxWidth="680" HorizontalAlignment="Left">
  <Border Style="{StaticResource Card}"><StackPanel><TextBlock Text="Подключение" Style="{StaticResource SectionTitle}"/><TextBlock x:Name="ServerAddressText" Style="{StaticResource Hint}"/><Button Content="Подключить или сменить сервер" HorizontalAlignment="Left" Click="ConnectClick"/><TextBlock Text="Вставьте код, выданный в LandErp. Локальные поиски и группы не отправляются на сервер автоматически." Style="{StaticResource Hint}"/></StackPanel></Border>
  <Border Style="{StaticResource Card}" Margin="0,10"><StackPanel><TextBlock Text="Сбор данных" Style="{StaticResource SectionTitle}"/><TextBlock Text="Браузер" Margin="0,8,0,0"/><ComboBox x:Name="BrowserInput"><ComboBoxItem Content="Google Chrome"/><ComboBoxItem Content="Microsoft Edge"/></ComboBox><TextBlock Text="Предел страниц для локального сбора"/><TextBox x:Name="MaxPagesInput"/><TextBlock Text="Не повторять свежие результаты при ручном запуске, часов"/><TextBox x:Name="FreshnessInput"/>
  <TextBlock Text="Открывать карточку объявления" Margin="0,8,0,0"/><ComboBox x:Name="ListingDetailsModeInput"><ComboBoxItem Content="Справа в рабочем окне"/><ComboBoxItem Content="В отдельном окне"/></ComboBox>
  <Button x:Name="SaveSettingsButton" Content="Сохранить настройки" Style="{StaticResource Primary}" HorizontalAlignment="Left" Click="SaveSettingsClick"/><TextBlock Text="Расписание выполняется только при открытом приложении и включённой автоработе. Сервер управляет своими расписаниями самостоятельно." Style="{StaticResource Hint}"/></StackPanel></Border>
  <Border Style="{StaticResource Card}"><StackPanel><TextBlock Text="Помощь" Style="{StaticResource SectionTitle}"/><TextBlock Text="Если сбор остановился с ошибкой, сохраните краткий отчёт для разбора." Style="{StaticResource Hint}"/><Button Content="Сохранить диагностику" HorizontalAlignment="Left" Click="DiagnosticsClick"/></StackPanel></Border>
</StackPanel></ScrollViewer></TabItem>
</TabControl>
</DockPanel>
</Window>
