# Интеграция веток, 2026-09-21

База: `main` `f554c5a`. Перенесены `460be656` (группы Catalog), `3939067` (доступ, расписания, осмотры, IIS) и parser-коммиты `53125474f9`, `bb5095cd9d`, `36cf074f55`, `d7edc8278a`. Старый вариант schedule/access/IIS, накопительные Package 02/03/04 и SDK-коммит целиком не переносились.

Затронуты Catalog/Procurement/Identity/Collection, их UI, две миграции, EF snapshot, локальный Parser и профильные тесты. В `RefreshJobs()` сохранён `JobProgress()` из `main` вместе с новым `SourceCountHint`; сохранена подпись лимита страниц. Snapshot содержит обе модели; в миграцию Catalog добавлен индекс, ожидаемый моделью EF. Исправлены несовместимости типов/UI и тестовых данных после объединения.

Проверки: locked restore с `NuGetAudit=false` (сетевой endpoint недоступен), Release build — 0 ошибок/предупреждений, EF model diff — 0 операций, 19 профильных Foundation тестов — Passed, `git diff --check` — Passed. Локальный Parser не тестировался по указанию владельца. HTTPS/browser тесты не засчитаны: на машине нет действительного dev-сертификата. Production migrations не применялись.

Следующий шаг: владелец проверяет собранный релиз, включая локальный Parser и HTTPS/browser сценарий на целевой машине. B4-04 требует отдельного запроса.
