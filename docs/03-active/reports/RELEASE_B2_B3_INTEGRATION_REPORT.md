# Интеграция Release B2 и B3-01–B3-02

**Дата:** 18 сентября 2026 года  
**Ветка:** `codex/integrate-b2-b3`  
**База:** `80750e29d11ac6f694d9d3724d3442a91c76cbc8`  
**Пакетный источник:** `codex/release-package-03` до `a500aec`

## Результат

B2-01–B2-04 и B3-01–B3-02 перенесены поверх актуальной карточки объекта.
Старая разметка пакетной ветки не заменяла действующий интерфейс: действия
исправления рабочих данных, связи источника и восстановления загрузки встроены
в существующие `CaseModal`, вкладки и документную таблицу.

Сохранены более поздние изменения `main`, включая право редактора досье добавлять
рабочие заметки. Migration и изменение схемы БД не потребовались.

## Исправления интеграции

- assertions аудита B2-01 больше не зависят от пробелов форматирования JSON;
- устранено предупреждение-анализатор B3-01;
- исправлена компиляция теста B3-02;
- Local storage использует content-addressed ключ и допускает безопасный повтор
  после записи provider без подтверждения БД;
- чтение прежних локальных ключей `GUID.bin` сохранено;
- SDK закреплён на установленной версии `10.0.401`.

## Проверки

- `dotnet build LandErp.slnx -c Release --no-restore` — Passed,
  0 предупреждений, 0 ошибок;
- целевые `ReleasePackageB201Tests`, `B202Tests`, `B301Tests`, `B302Tests` —
  Passed, 16/16;
- сборка Server в тестовой рабочей копии — Passed, 0 предупреждений, 0 ошибок;
- `GET /health/ready` — HTTP 200, `ready`.

Server запущен на `https://localhost:7240` с прежней локальной БД и приватной
конфигурацией хранилища. Ручная проверка интерфейса владельцем остаётся частью
приёмки; автоматическая миграция при запуске не выполнялась.
